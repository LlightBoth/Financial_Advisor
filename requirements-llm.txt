# Dependencies to run the local Financial Advisor AI model server (training/serve_llm.py)
torch>=2.2.0
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.28.0
# Optional for 4-bit NVIDIA GPU quantization:
# bitsandbytes>=0.43.0
