# Security
SECRET_KEY=dev_secret_key_financial_advisor_demo

# Optional: Google OAuth (leave empty to use username/password login)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:5005/auth/google/callback

# Optional: Redis URL for rate limiting (falls back to in-memory if empty)
# REDIS_URL=redis://localhost:6379/0
